import { KpiAggregate } from '@/shared/types'

export async function getPublishedKpiAggregates(): Promise<KpiAggregate[]> {
  // Placeholder for Payload query integration.
  // Returns empty by default to avoid fake KPI data.
  return []
}
